node_modules
dist
dist-ssr
public/tilawa/
*.local
.claude/settings.local.json
.vite
.DS_Store
npm-debug.log*
.env
.env.*
!.env.example
tmp-*
*.tsbuildinfo

# Generated single-file preview (npm run preview:artifact)
docs/tahqeeq-preview.html

# Private transcript attachment for local handoff
TAHQEEQ_BUILD_TRANSCRIPT.md

# Local private data and scratch work; never include these in a release.
/.private/
/output/
/tmp/

# outputs/ contains tracked Mushaf source inputs. Keep it available and stage
# only explicitly reviewed files from that directory.
